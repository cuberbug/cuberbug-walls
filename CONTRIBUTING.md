<h1 align="center">🎋 🎋 🎋 ₍^. ̫ .^₎⟆ 🎋 🎋 🎋</h1>

## 🐾 Вклад

Вы можете поучаствовать в развитии колекции и предложить Pull Requests со своими изображениями! Чтобы не возникло путаницы, добавляйте их в директорию `contrib`.

```shell
/cuberbug-walls/
└── wallpapers
    └── contrib  # для предложений
```

Если всё хорошо, то я с удовольствием приму ваше предложение и самостоятельно встрою его в коллекцию, сделав апскейлинг и сжатие при необходимости.

### 🐾 Подсказка

Сделайте форк: у вас появится копия этого репозитория с выбранным вами названием. Затем клонируйте ваш новый репозиторий, после чего добавьте в него апстрим:
```shell
git remote add upstream https://github.com/cuberbug/cuberbug-walls.git
```
#### 🐾 Синхронизация с `upstream`

Вытянуть актуальное состояние из основного репозитория:
```shell
git pull upstream main
```

Зафиксировать изменения в своём форке:
```shell
git push origin main
```

#### 🐾 Создание своей ветки

Создайте свою ветку — она нужна вам для того, чтобы предложить свой вклад. Пример команды для создания:
```shell
git switch -c meow
```

Эта команда создаст ветку `meow` в вашем репозитории и сразу переключится на неё. В дальнейшем для переключения между ветками можно использовать ту же команду, но уже без флага `-c`, который отвечает за создание (create) новой ветки:
```shell
git switch main  # пример
git switch meow  # пример
```

Добавить новую ветку в удалённый репозиторий:
```shell
git push -u origin meow
```
Для последующих коммитов достаточно будет использовать `git push`.

#### 🐾 Magick Time! ✨

Добавьте свои изображения в `/wallpapers/contrib/` 👀

#### 🐾 Что дальше?

Дело за малым:
* перейти в веб-интерфейс GitHub на страничку своего форка
* зайти во вкладку "Pull requests" и нажать "New pull request"
* выбрать в качестве адресата меня, то есть `cuberbug/cuberbug-walls` и предложить свою ветку, например `meow`
* вместе с PR можете оставить мне последие, я его обязательно прочту! ✍️

#### 🐾 А потом?

Если что-то произошло, а ветка, с которой вы работали, больше вам не нужна, то её можно удалить локально:
```shell
git branch -d meow
```

И отправить изменения в свой удалённый репозиторий:
```shell
git push origin --delete meow
```

<h1 align="center">❤️ ₍^. ̫ .^₎⟆ ❤️</h1>

Спасибо за участие!